# Lab Report Processor

Express service that accepts lab report PDFs or images, extracts text when possible, and forwards it to AI Layer Core.

## Start

```bash
npm install && copy .env.example .env && npm run dev
```
